package archknife.fragment

import androidx.fragment.app.Fragment

/**
 * This empty Fragment object is used to attach a default Fragment to each Activity (if none is
 * given). This is required to resolve a clean and consistent dagger structure.
 *
 * @since 1.0
 */
class EmptyFragment: Fragment()